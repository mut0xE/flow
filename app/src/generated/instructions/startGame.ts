/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { FLOW_PROGRAM_ADDRESS } from "../programs";

export const START_GAME_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  249, 47, 252, 172, 184, 162, 245, 14,
]);

export function getStartGameDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(START_GAME_DISCRIMINATOR);
}

export type StartGameInstruction<
  TProgram extends string = typeof FLOW_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountGame extends string | AccountMeta<string> = string,
  TAccountCreatorPlayer extends string | AccountMeta<string> = string,
  TAccountPriceFeed extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountGame extends string
        ? WritableAccount<TAccountGame>
        : TAccountGame,
      TAccountCreatorPlayer extends string
        ? WritableAccount<TAccountCreatorPlayer>
        : TAccountCreatorPlayer,
      TAccountPriceFeed extends string
        ? ReadonlyAccount<TAccountPriceFeed>
        : TAccountPriceFeed,
      ...TRemainingAccounts,
    ]
  >;

export type StartGameInstructionData = { discriminator: ReadonlyUint8Array };

export type StartGameInstructionDataArgs = {};

export function getStartGameInstructionDataEncoder(): FixedSizeEncoder<StartGameInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: START_GAME_DISCRIMINATOR }),
  );
}

export function getStartGameInstructionDataDecoder(): FixedSizeDecoder<StartGameInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getStartGameInstructionDataCodec(): FixedSizeCodec<
  StartGameInstructionDataArgs,
  StartGameInstructionData
> {
  return combineCodec(
    getStartGameInstructionDataEncoder(),
    getStartGameInstructionDataDecoder(),
  );
}

export type StartGameInput<
  TAccountSigner extends string = string,
  TAccountGame extends string = string,
  TAccountCreatorPlayer extends string = string,
  TAccountPriceFeed extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  game: Address<TAccountGame>;
  creatorPlayer: Address<TAccountCreatorPlayer>;
  priceFeed: Address<TAccountPriceFeed>;
};

export function getStartGameInstruction<
  TAccountSigner extends string,
  TAccountGame extends string,
  TAccountCreatorPlayer extends string,
  TAccountPriceFeed extends string,
  TProgramAddress extends Address = typeof FLOW_PROGRAM_ADDRESS,
>(
  input: StartGameInput<
    TAccountSigner,
    TAccountGame,
    TAccountCreatorPlayer,
    TAccountPriceFeed
  >,
  config?: { programAddress?: TProgramAddress },
): StartGameInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountGame,
  TAccountCreatorPlayer,
  TAccountPriceFeed
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLOW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    game: { value: input.game ?? null, isWritable: true },
    creatorPlayer: { value: input.creatorPlayer ?? null, isWritable: true },
    priceFeed: { value: input.priceFeed ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("game", accounts.game),
      getAccountMeta("creatorPlayer", accounts.creatorPlayer),
      getAccountMeta("priceFeed", accounts.priceFeed),
    ],
    data: getStartGameInstructionDataEncoder().encode({}),
    programAddress,
  } as StartGameInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountGame,
    TAccountCreatorPlayer,
    TAccountPriceFeed
  >);
}

export type ParsedStartGameInstruction<
  TProgram extends string = typeof FLOW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    game: TAccountMetas[1];
    creatorPlayer: TAccountMetas[2];
    priceFeed: TAccountMetas[3];
  };
  data: StartGameInstructionData;
};

export function parseStartGameInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedStartGameInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      game: getNextAccount(),
      creatorPlayer: getNextAccount(),
      priceFeed: getNextAccount(),
    },
    data: getStartGameInstructionDataDecoder().decode(instruction.data),
  };
}
