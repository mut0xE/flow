/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { FLOW_PROGRAM_ADDRESS } from "../programs";

export const TICK_PRICE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  237, 206, 163, 122, 103, 44, 37, 112,
]);

export function getTickPriceDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(TICK_PRICE_DISCRIMINATOR);
}

export type TickPriceInstruction<
  TProgram extends string = typeof FLOW_PROGRAM_ADDRESS,
  TAccountGameState extends string | AccountMeta<string> = string,
  TAccountPriceFeed extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGameState extends string
        ? WritableAccount<TAccountGameState>
        : TAccountGameState,
      TAccountPriceFeed extends string
        ? ReadonlyAccount<TAccountPriceFeed>
        : TAccountPriceFeed,
      ...TRemainingAccounts,
    ]
  >;

export type TickPriceInstructionData = { discriminator: ReadonlyUint8Array };

export type TickPriceInstructionDataArgs = {};

export function getTickPriceInstructionDataEncoder(): FixedSizeEncoder<TickPriceInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: TICK_PRICE_DISCRIMINATOR }),
  );
}

export function getTickPriceInstructionDataDecoder(): FixedSizeDecoder<TickPriceInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getTickPriceInstructionDataCodec(): FixedSizeCodec<
  TickPriceInstructionDataArgs,
  TickPriceInstructionData
> {
  return combineCodec(
    getTickPriceInstructionDataEncoder(),
    getTickPriceInstructionDataDecoder(),
  );
}

export type TickPriceInput<
  TAccountGameState extends string = string,
  TAccountPriceFeed extends string = string,
> = {
  gameState: Address<TAccountGameState>;
  priceFeed: Address<TAccountPriceFeed>;
};

export function getTickPriceInstruction<
  TAccountGameState extends string,
  TAccountPriceFeed extends string,
  TProgramAddress extends Address = typeof FLOW_PROGRAM_ADDRESS,
>(
  input: TickPriceInput<TAccountGameState, TAccountPriceFeed>,
  config?: { programAddress?: TProgramAddress },
): TickPriceInstruction<TProgramAddress, TAccountGameState, TAccountPriceFeed> {
  // Program address.
  const programAddress = config?.programAddress ?? FLOW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    gameState: { value: input.gameState ?? null, isWritable: true },
    priceFeed: { value: input.priceFeed ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("gameState", accounts.gameState),
      getAccountMeta("priceFeed", accounts.priceFeed),
    ],
    data: getTickPriceInstructionDataEncoder().encode({}),
    programAddress,
  } as TickPriceInstruction<
    TProgramAddress,
    TAccountGameState,
    TAccountPriceFeed
  >);
}

export type ParsedTickPriceInstruction<
  TProgram extends string = typeof FLOW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    gameState: TAccountMetas[0];
    priceFeed: TAccountMetas[1];
  };
  data: TickPriceInstructionData;
};

export function parseTickPriceInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedTickPriceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { gameState: getNextAccount(), priceFeed: getNextAccount() },
    data: getTickPriceInstructionDataDecoder().decode(instruction.data),
  };
}
