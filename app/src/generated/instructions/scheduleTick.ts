/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findGamePda } from "../pdas";
import { FLOW_PROGRAM_ADDRESS } from "../programs";

export const SCHEDULE_TICK_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  197, 80, 214, 198, 209, 150, 211, 44,
]);

export function getScheduleTickDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SCHEDULE_TICK_DISCRIMINATOR,
  );
}

export type ScheduleTickInstruction<
  TProgram extends string = typeof FLOW_PROGRAM_ADDRESS,
  TAccountMagicProgram extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountGame extends string | AccountMeta<string> = string,
  TAccountPriceFeed extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMagicProgram extends string
        ? ReadonlyAccount<TAccountMagicProgram>
        : TAccountMagicProgram,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountCreator extends string
        ? ReadonlyAccount<TAccountCreator>
        : TAccountCreator,
      TAccountGame extends string
        ? WritableAccount<TAccountGame>
        : TAccountGame,
      TAccountPriceFeed extends string
        ? ReadonlyAccount<TAccountPriceFeed>
        : TAccountPriceFeed,
      TAccountProgram extends string
        ? ReadonlyAccount<TAccountProgram>
        : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ScheduleTickInstructionData = {
  discriminator: ReadonlyUint8Array;
  gameId: bigint;
  taskId: bigint;
  executionIntervalMillis: bigint;
  iterations: bigint;
};

export type ScheduleTickInstructionDataArgs = {
  gameId: number | bigint;
  taskId: number | bigint;
  executionIntervalMillis: number | bigint;
  iterations: number | bigint;
};

export function getScheduleTickInstructionDataEncoder(): FixedSizeEncoder<ScheduleTickInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["gameId", getU64Encoder()],
      ["taskId", getI64Encoder()],
      ["executionIntervalMillis", getI64Encoder()],
      ["iterations", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SCHEDULE_TICK_DISCRIMINATOR }),
  );
}

export function getScheduleTickInstructionDataDecoder(): FixedSizeDecoder<ScheduleTickInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["gameId", getU64Decoder()],
    ["taskId", getI64Decoder()],
    ["executionIntervalMillis", getI64Decoder()],
    ["iterations", getI64Decoder()],
  ]);
}

export function getScheduleTickInstructionDataCodec(): FixedSizeCodec<
  ScheduleTickInstructionDataArgs,
  ScheduleTickInstructionData
> {
  return combineCodec(
    getScheduleTickInstructionDataEncoder(),
    getScheduleTickInstructionDataDecoder(),
  );
}

export type ScheduleTickAsyncInput<
  TAccountMagicProgram extends string = string,
  TAccountPayer extends string = string,
  TAccountCreator extends string = string,
  TAccountGame extends string = string,
  TAccountPriceFeed extends string = string,
  TAccountProgram extends string = string,
> = {
  magicProgram: Address<TAccountMagicProgram>;
  /** session key or any signer — not used for seed derivation */
  payer: TransactionSigner<TAccountPayer>;
  creator: Address<TAccountCreator>;
  game?: Address<TAccountGame>;
  priceFeed: Address<TAccountPriceFeed>;
  program: Address<TAccountProgram>;
  gameId: ScheduleTickInstructionDataArgs["gameId"];
  taskId: ScheduleTickInstructionDataArgs["taskId"];
  executionIntervalMillis: ScheduleTickInstructionDataArgs["executionIntervalMillis"];
  iterations: ScheduleTickInstructionDataArgs["iterations"];
};

export async function getScheduleTickInstructionAsync<
  TAccountMagicProgram extends string,
  TAccountPayer extends string,
  TAccountCreator extends string,
  TAccountGame extends string,
  TAccountPriceFeed extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof FLOW_PROGRAM_ADDRESS,
>(
  input: ScheduleTickAsyncInput<
    TAccountMagicProgram,
    TAccountPayer,
    TAccountCreator,
    TAccountGame,
    TAccountPriceFeed,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ScheduleTickInstruction<
    TProgramAddress,
    TAccountMagicProgram,
    TAccountPayer,
    TAccountCreator,
    TAccountGame,
    TAccountPriceFeed,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLOW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    magicProgram: { value: input.magicProgram ?? null, isWritable: false },
    payer: { value: input.payer ?? null, isWritable: true },
    creator: { value: input.creator ?? null, isWritable: false },
    game: { value: input.game ?? null, isWritable: true },
    priceFeed: { value: input.priceFeed ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.game.value) {
    accounts.game.value = await findGamePda({
      gameId: getNonNullResolvedInstructionInput("gameId", args.gameId),
      creator: getAddressFromResolvedInstructionAccount(
        "creator",
        accounts.creator.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("magicProgram", accounts.magicProgram),
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("game", accounts.game),
      getAccountMeta("priceFeed", accounts.priceFeed),
      getAccountMeta("program", accounts.program),
    ],
    data: getScheduleTickInstructionDataEncoder().encode(
      args as ScheduleTickInstructionDataArgs,
    ),
    programAddress,
  } as ScheduleTickInstruction<
    TProgramAddress,
    TAccountMagicProgram,
    TAccountPayer,
    TAccountCreator,
    TAccountGame,
    TAccountPriceFeed,
    TAccountProgram
  >);
}

export type ScheduleTickInput<
  TAccountMagicProgram extends string = string,
  TAccountPayer extends string = string,
  TAccountCreator extends string = string,
  TAccountGame extends string = string,
  TAccountPriceFeed extends string = string,
  TAccountProgram extends string = string,
> = {
  magicProgram: Address<TAccountMagicProgram>;
  /** session key or any signer — not used for seed derivation */
  payer: TransactionSigner<TAccountPayer>;
  creator: Address<TAccountCreator>;
  game: Address<TAccountGame>;
  priceFeed: Address<TAccountPriceFeed>;
  program: Address<TAccountProgram>;
  gameId: ScheduleTickInstructionDataArgs["gameId"];
  taskId: ScheduleTickInstructionDataArgs["taskId"];
  executionIntervalMillis: ScheduleTickInstructionDataArgs["executionIntervalMillis"];
  iterations: ScheduleTickInstructionDataArgs["iterations"];
};

export function getScheduleTickInstruction<
  TAccountMagicProgram extends string,
  TAccountPayer extends string,
  TAccountCreator extends string,
  TAccountGame extends string,
  TAccountPriceFeed extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof FLOW_PROGRAM_ADDRESS,
>(
  input: ScheduleTickInput<
    TAccountMagicProgram,
    TAccountPayer,
    TAccountCreator,
    TAccountGame,
    TAccountPriceFeed,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ScheduleTickInstruction<
  TProgramAddress,
  TAccountMagicProgram,
  TAccountPayer,
  TAccountCreator,
  TAccountGame,
  TAccountPriceFeed,
  TAccountProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLOW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    magicProgram: { value: input.magicProgram ?? null, isWritable: false },
    payer: { value: input.payer ?? null, isWritable: true },
    creator: { value: input.creator ?? null, isWritable: false },
    game: { value: input.game ?? null, isWritable: true },
    priceFeed: { value: input.priceFeed ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("magicProgram", accounts.magicProgram),
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("game", accounts.game),
      getAccountMeta("priceFeed", accounts.priceFeed),
      getAccountMeta("program", accounts.program),
    ],
    data: getScheduleTickInstructionDataEncoder().encode(
      args as ScheduleTickInstructionDataArgs,
    ),
    programAddress,
  } as ScheduleTickInstruction<
    TProgramAddress,
    TAccountMagicProgram,
    TAccountPayer,
    TAccountCreator,
    TAccountGame,
    TAccountPriceFeed,
    TAccountProgram
  >);
}

export type ParsedScheduleTickInstruction<
  TProgram extends string = typeof FLOW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    magicProgram: TAccountMetas[0];
    /** session key or any signer — not used for seed derivation */
    payer: TAccountMetas[1];
    creator: TAccountMetas[2];
    game: TAccountMetas[3];
    priceFeed: TAccountMetas[4];
    program: TAccountMetas[5];
  };
  data: ScheduleTickInstructionData;
};

export function parseScheduleTickInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedScheduleTickInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      magicProgram: getNextAccount(),
      payer: getNextAccount(),
      creator: getNextAccount(),
      game: getNextAccount(),
      priceFeed: getNextAccount(),
      program: getNextAccount(),
    },
    data: getScheduleTickInstructionDataDecoder().decode(instruction.data),
  };
}
