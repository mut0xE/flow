/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { FLOW_PROGRAM_ADDRESS } from "../programs";

export const COMMIT_AND_SETTLE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([82, 154, 16, 183, 6, 4, 212, 85]);

export function getCommitAndSettleDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    COMMIT_AND_SETTLE_DISCRIMINATOR,
  );
}

export type CommitAndSettleInstruction<
  TProgram extends string = typeof FLOW_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountGame extends string | AccountMeta<string> = string,
  TAccountPriceFeed extends string | AccountMeta<string> = string,
  TAccountMagicProgram extends string | AccountMeta<string> =
    "Magic11111111111111111111111111111111111111",
  TAccountMagicContext extends string | AccountMeta<string> =
    "MagicContext1111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountGame extends string
        ? WritableAccount<TAccountGame>
        : TAccountGame,
      TAccountPriceFeed extends string
        ? ReadonlyAccount<TAccountPriceFeed>
        : TAccountPriceFeed,
      TAccountMagicProgram extends string
        ? ReadonlyAccount<TAccountMagicProgram>
        : TAccountMagicProgram,
      TAccountMagicContext extends string
        ? WritableAccount<TAccountMagicContext>
        : TAccountMagicContext,
      ...TRemainingAccounts,
    ]
  >;

export type CommitAndSettleInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CommitAndSettleInstructionDataArgs = {};

export function getCommitAndSettleInstructionDataEncoder(): FixedSizeEncoder<CommitAndSettleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: COMMIT_AND_SETTLE_DISCRIMINATOR }),
  );
}

export function getCommitAndSettleInstructionDataDecoder(): FixedSizeDecoder<CommitAndSettleInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCommitAndSettleInstructionDataCodec(): FixedSizeCodec<
  CommitAndSettleInstructionDataArgs,
  CommitAndSettleInstructionData
> {
  return combineCodec(
    getCommitAndSettleInstructionDataEncoder(),
    getCommitAndSettleInstructionDataDecoder(),
  );
}

export type CommitAndSettleInput<
  TAccountSigner extends string = string,
  TAccountGame extends string = string,
  TAccountPriceFeed extends string = string,
  TAccountMagicProgram extends string = string,
  TAccountMagicContext extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  game: Address<TAccountGame>;
  priceFeed: Address<TAccountPriceFeed>;
  magicProgram?: Address<TAccountMagicProgram>;
  magicContext?: Address<TAccountMagicContext>;
};

export function getCommitAndSettleInstruction<
  TAccountSigner extends string,
  TAccountGame extends string,
  TAccountPriceFeed extends string,
  TAccountMagicProgram extends string,
  TAccountMagicContext extends string,
  TProgramAddress extends Address = typeof FLOW_PROGRAM_ADDRESS,
>(
  input: CommitAndSettleInput<
    TAccountSigner,
    TAccountGame,
    TAccountPriceFeed,
    TAccountMagicProgram,
    TAccountMagicContext
  >,
  config?: { programAddress?: TProgramAddress },
): CommitAndSettleInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountGame,
  TAccountPriceFeed,
  TAccountMagicProgram,
  TAccountMagicContext
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLOW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    game: { value: input.game ?? null, isWritable: true },
    priceFeed: { value: input.priceFeed ?? null, isWritable: false },
    magicProgram: { value: input.magicProgram ?? null, isWritable: false },
    magicContext: { value: input.magicContext ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.magicProgram.value) {
    accounts.magicProgram.value =
      "Magic11111111111111111111111111111111111111" as Address<"Magic11111111111111111111111111111111111111">;
  }
  if (!accounts.magicContext.value) {
    accounts.magicContext.value =
      "MagicContext1111111111111111111111111111111" as Address<"MagicContext1111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("game", accounts.game),
      getAccountMeta("priceFeed", accounts.priceFeed),
      getAccountMeta("magicProgram", accounts.magicProgram),
      getAccountMeta("magicContext", accounts.magicContext),
    ],
    data: getCommitAndSettleInstructionDataEncoder().encode({}),
    programAddress,
  } as CommitAndSettleInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountGame,
    TAccountPriceFeed,
    TAccountMagicProgram,
    TAccountMagicContext
  >);
}

export type ParsedCommitAndSettleInstruction<
  TProgram extends string = typeof FLOW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    game: TAccountMetas[1];
    priceFeed: TAccountMetas[2];
    magicProgram: TAccountMetas[3];
    magicContext: TAccountMetas[4];
  };
  data: CommitAndSettleInstructionData;
};

export function parseCommitAndSettleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCommitAndSettleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      game: getNextAccount(),
      priceFeed: getNextAccount(),
      magicProgram: getNextAccount(),
      magicContext: getNextAccount(),
    },
    data: getCommitAndSettleInstructionDataDecoder().decode(instruction.data),
  };
}
