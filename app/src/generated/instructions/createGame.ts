/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findGamePda, findPlayerPda, findVaultPda } from "../pdas";
import { FLOW_PROGRAM_ADDRESS } from "../programs";
import {
  getDirectionDecoder,
  getDirectionEncoder,
  type Direction,
  type DirectionArgs,
} from "../types";

export const CREATE_GAME_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  124, 69, 75, 66, 184, 220, 72, 206,
]);

export function getCreateGameDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CREATE_GAME_DISCRIMINATOR);
}

export type CreateGameInstruction<
  TProgram extends string = typeof FLOW_PROGRAM_ADDRESS,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountGame extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountPlayer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCreator extends string
        ? WritableSignerAccount<TAccountCreator> &
            AccountSignerMeta<TAccountCreator>
        : TAccountCreator,
      TAccountGame extends string
        ? WritableAccount<TAccountGame>
        : TAccountGame,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountPlayer extends string
        ? WritableAccount<TAccountPlayer>
        : TAccountPlayer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateGameInstructionData = {
  discriminator: ReadonlyUint8Array;
  gameId: bigint;
  direction: Direction;
  entryFee: bigint;
  lossLimit: number;
  maxPlayers: number;
  endsAt: bigint;
};

export type CreateGameInstructionDataArgs = {
  gameId: number | bigint;
  direction: DirectionArgs;
  entryFee: number | bigint;
  lossLimit: number;
  maxPlayers: number;
  endsAt: number | bigint;
};

export function getCreateGameInstructionDataEncoder(): FixedSizeEncoder<CreateGameInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["gameId", getU64Encoder()],
      ["direction", getDirectionEncoder()],
      ["entryFee", getU64Encoder()],
      ["lossLimit", getU8Encoder()],
      ["maxPlayers", getU8Encoder()],
      ["endsAt", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_GAME_DISCRIMINATOR }),
  );
}

export function getCreateGameInstructionDataDecoder(): FixedSizeDecoder<CreateGameInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["gameId", getU64Decoder()],
    ["direction", getDirectionDecoder()],
    ["entryFee", getU64Decoder()],
    ["lossLimit", getU8Decoder()],
    ["maxPlayers", getU8Decoder()],
    ["endsAt", getI64Decoder()],
  ]);
}

export function getCreateGameInstructionDataCodec(): FixedSizeCodec<
  CreateGameInstructionDataArgs,
  CreateGameInstructionData
> {
  return combineCodec(
    getCreateGameInstructionDataEncoder(),
    getCreateGameInstructionDataDecoder(),
  );
}

export type CreateGameAsyncInput<
  TAccountCreator extends string = string,
  TAccountGame extends string = string,
  TAccountVault extends string = string,
  TAccountPlayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  creator: TransactionSigner<TAccountCreator>;
  game?: Address<TAccountGame>;
  vault?: Address<TAccountVault>;
  player?: Address<TAccountPlayer>;
  systemProgram?: Address<TAccountSystemProgram>;
  gameId: CreateGameInstructionDataArgs["gameId"];
  direction: CreateGameInstructionDataArgs["direction"];
  entryFee: CreateGameInstructionDataArgs["entryFee"];
  lossLimit: CreateGameInstructionDataArgs["lossLimit"];
  maxPlayers: CreateGameInstructionDataArgs["maxPlayers"];
  endsAt: CreateGameInstructionDataArgs["endsAt"];
};

export async function getCreateGameInstructionAsync<
  TAccountCreator extends string,
  TAccountGame extends string,
  TAccountVault extends string,
  TAccountPlayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FLOW_PROGRAM_ADDRESS,
>(
  input: CreateGameAsyncInput<
    TAccountCreator,
    TAccountGame,
    TAccountVault,
    TAccountPlayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateGameInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountGame,
    TAccountVault,
    TAccountPlayer,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLOW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    creator: { value: input.creator ?? null, isWritable: true },
    game: { value: input.game ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.game.value) {
    accounts.game.value = await findGamePda({
      gameId: getNonNullResolvedInstructionInput("gameId", args.gameId),
      creator: getAddressFromResolvedInstructionAccount(
        "creator",
        accounts.creator.value,
      ),
    });
  }
  if (!accounts.vault.value) {
    accounts.vault.value = await findVaultPda({
      game: getAddressFromResolvedInstructionAccount(
        "game",
        accounts.game.value,
      ),
    });
  }
  if (!accounts.player.value) {
    accounts.player.value = await findPlayerPda({
      game: getAddressFromResolvedInstructionAccount(
        "game",
        accounts.game.value,
      ),
      creator: getAddressFromResolvedInstructionAccount(
        "creator",
        accounts.creator.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("game", accounts.game),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("player", accounts.player),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateGameInstructionDataEncoder().encode(
      args as CreateGameInstructionDataArgs,
    ),
    programAddress,
  } as CreateGameInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountGame,
    TAccountVault,
    TAccountPlayer,
    TAccountSystemProgram
  >);
}

export type CreateGameInput<
  TAccountCreator extends string = string,
  TAccountGame extends string = string,
  TAccountVault extends string = string,
  TAccountPlayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  creator: TransactionSigner<TAccountCreator>;
  game: Address<TAccountGame>;
  vault: Address<TAccountVault>;
  player: Address<TAccountPlayer>;
  systemProgram?: Address<TAccountSystemProgram>;
  gameId: CreateGameInstructionDataArgs["gameId"];
  direction: CreateGameInstructionDataArgs["direction"];
  entryFee: CreateGameInstructionDataArgs["entryFee"];
  lossLimit: CreateGameInstructionDataArgs["lossLimit"];
  maxPlayers: CreateGameInstructionDataArgs["maxPlayers"];
  endsAt: CreateGameInstructionDataArgs["endsAt"];
};

export function getCreateGameInstruction<
  TAccountCreator extends string,
  TAccountGame extends string,
  TAccountVault extends string,
  TAccountPlayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FLOW_PROGRAM_ADDRESS,
>(
  input: CreateGameInput<
    TAccountCreator,
    TAccountGame,
    TAccountVault,
    TAccountPlayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateGameInstruction<
  TProgramAddress,
  TAccountCreator,
  TAccountGame,
  TAccountVault,
  TAccountPlayer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLOW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    creator: { value: input.creator ?? null, isWritable: true },
    game: { value: input.game ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("game", accounts.game),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("player", accounts.player),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateGameInstructionDataEncoder().encode(
      args as CreateGameInstructionDataArgs,
    ),
    programAddress,
  } as CreateGameInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountGame,
    TAccountVault,
    TAccountPlayer,
    TAccountSystemProgram
  >);
}

export type ParsedCreateGameInstruction<
  TProgram extends string = typeof FLOW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    creator: TAccountMetas[0];
    game: TAccountMetas[1];
    vault: TAccountMetas[2];
    player: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: CreateGameInstructionData;
};

export function parseCreateGameInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateGameInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      creator: getNextAccount(),
      game: getNextAccount(),
      vault: getNextAccount(),
      player: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateGameInstructionDataDecoder().decode(instruction.data),
  };
}
